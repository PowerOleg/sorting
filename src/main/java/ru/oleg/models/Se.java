package ru.oleg.models;

public class Se extends Item {
    public Se(String type, int positionNumber, int systemNumber, int number, String line) {
        super(type, positionNumber, systemNumber, number, line);
    }
}
